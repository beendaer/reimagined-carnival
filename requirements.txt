# Python 3.11+ Compatible Dependencies
# Production-ready with version pins for stability

# Web Framework
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0,<0.41.0
fastapi>=0.115.0,<0.130.0
uvicorn[standard]>=0.32.0,<0.33.0
slowapi>=0.1.9,<0.2.0

# HTTP Clients
requests>=2.32.0,<3.0.0
httpx>=0.27.0,<0.29.0

# Form Data (Security: Updated to 0.0.22+ for CVE fixes)
python-multipart>=0.0.22,<0.1.0

# Database (PostgreSQL prepared - not yet used)
psycopg2-binary>=2.9.9,<3.0.0

# Caching (Redis prepared - not yet used)
redis>=5.2.0,<6.0.0

# Environment & Configuration
python-dotenv>=1.0.0,<2.0.0

# Production Server Enhancements
gunicorn>=23.0.0,<26.0.0
